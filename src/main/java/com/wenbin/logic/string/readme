字符串匹配高级算法：
由暴力法开始各种优化：
1.Rabin-karp算法
概要： 暴力算法中，我们需要挨个比较所有字符，才知道是否有匹配的字串。Rabin-karp算法通过对字串进行hash来避免无谓的暴力比较。
步骤：
1)计算子串（M）的hash值
2)计算大串（N）中每个字串的hash值，需要计算N-M+1次
3)比较hash值，如果不想等，肯定不匹配，继续下一轮，如果相等，再暴力循环比较。

ps：hash的算法要注意，特别定义hash算法，可以通过减一个数减哈希值，加一个数加哈希值的方式来实现hash计算
否则还是没有减少整体的时间复杂度，因为计算哈希时还会遍历


2.KMP算法
概要：当字串与目标字符串不匹配时，我们已经知道前面匹配成功的那一部分字符了。我们可以利用这个已知信息，忽略已经比较过的位置来提高效率
参考：http://www.ruanyifeng.com/blog/2013/05/Knuth%E2%80%93Morris%E2%80%93Pratt_algorithm.html




3.还有一些算法